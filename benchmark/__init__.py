"""
Graph_Benchmarking — top-level package.

Sub-packages:
    connectors  — thin connection wrappers for each database platform
    benchmark   — workload runners and harness (future phases)
    analysis    — result aggregation and reporting (future phases)
"""
